/** shortcuts 도메인 공개 API — 외부에서는 이 배럴로만 import한다. */
export { acceleratorSymbols, buildAccelerator, matchesAccelerator } from "./accelerator";
export { ShortcutEditor, styles } from "./shortcut-editor";
